name: Keep Streamlit App Alive

on:
  schedule:
    - cron: '0 0,12 * * *'
  workflow_dispatch:

jobs:
  keep-alive:
    runs-on: ubuntu-latest
    steps:
      - name: Ping the app
        run: curl -s https://is-it-pathogenic.streamlit.app/ > /dev/null
